@extends("layouts.app") @section("title", "Add User") @section("content") <div class="p-6"><h1>Add User</h1><form method="POST" action="{{ route("users.store") }}">@csrf <input name="name" placeholder="Name" required> <input name="email" type="email" placeholder="Email" required> <input name="password" type="password" placeholder="Password" required> <select name="role_id"><option value="">Select Role</option>@foreach(\App\Models\Role::all() as $role)<option value="{{ $role->id }}">{{ $role->display_name }}</option>@endforeach</select> <button type="submit">Create User</button></form></div> @endsection
