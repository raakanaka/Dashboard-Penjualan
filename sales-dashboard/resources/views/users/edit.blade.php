@extends("layouts.app") @section("title", "Edit User") @section("content") <div class="p-6"><h1>Edit User</h1><form method="POST" action="{{ route("users.update", $user) }}">@csrf @method("PUT") <input name="name" value="{{ $user->name }}" required> <input name="email" value="{{ $user->email }}" required> <select name="role_id">@foreach(\App\Models\Role::all() as $role)<option value="{{ $role->id }}" {{ $user->role_id == $role->id ? "selected" : "" }}>{{ $role->display_name }}</option>@endforeach</select> <button type="submit">Update User</button></form></div> @endsection
